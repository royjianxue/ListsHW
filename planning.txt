Add students to a class roster List until there are no more students. Then print out the count of the students to the Console.

bool keepGOing = false;
Create an empty {class} list,

do
{
	ask user input for student name {firstName}
	
	add {firstName} to {class} List
	
	ask user if there is anymore student needs to be entered?
	
	if(answer uppercase is YES)
		keepGOing = true
	
	else
		keepGoing = false




}while()
